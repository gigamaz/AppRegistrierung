# Nächste Schritte

1. `AppRegistrierung.ps1` im echten Ablauf testen, besonders Owner-Suche, Benutzererstellung, App-Erstellung, Redirect URI und CSV-Export.

2. `README.md` und `SESSION_HANDOFF.md` nur noch bei Abweichungen vom tatsächlichen Skriptstand anpassen.

3. Prüfen, ob der CSV-Export im gewünschten Ordner landet und die Spalten `App-Name`, `App ID`, `Tenant ID` und `Owner` enthält.

4. Offene Fehler oder Verbesserungen direkt in `SESSION_HANDOFF.md` festhalten.

5. Erst danach funktionale Änderungen am Skript vornehmen.
